# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — ignore every .env variant except the committed example
.env
.env.*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# built-in CRM disk persistence (opt-in via BUILTIN_PERSIST)
/.data

# local visual-QA tooling (headless screenshots; not part of the app)
/.shot.mjs
/shot-*.png

# Staged on-device voice bundle (recreated from node_modules at build time)
public/vendor/
