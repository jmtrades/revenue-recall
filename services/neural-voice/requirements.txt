# In-house neural voice service deps. All permissively licensed, all self-hosted.
#
# Default engine is Kokoro (VOICE_ENGINE=kokoro): an 82M-param model that ranks
# at the top of open TTS leaderboards, Apache-2.0, runs on CPU faster than
# real-time, native 24kHz, 54 voices. Piper is kept as a lighter fallback
# (VOICE_ENGINE=piper).
kokoro-onnx>=0.5.0    # frontier-class open neural TTS (Apache-2.0), ONNX — no torch
piper-tts>=1.2.0      # lighter VITS fallback engine (MIT)
chatterbox-tts>=0.1.7 # per-rep voice cloning (Resemble AI, MIT) — watermarked output
websockets>=12.0      # streaming PCM transport to the web app
numpy>=1.24
scipy>=1.10           # polyphase resampling (native -> web 24k / phone 8k)
soundfile>=0.12       # wav io for the offline `render` helper
